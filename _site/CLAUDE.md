# Instructions for Claude

## Git workflow
- **NEVER push to remote unless Jamie explicitly tells you to push.**
- You can commit changes, but always wait for explicit permission before running `git push`.
